version: "3.8"

services:
  continuum:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REPLICAS=150
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/nodes"]
      interval: 30s
      timeout: 5s
      retries: 3
